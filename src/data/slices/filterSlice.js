const initialData = {
    status: 'active',
    colors:[]
};

export default function fliterReducer(state=initialData, action)
{
    switch(action.type)
    {
        default: 
            return state
    }
}